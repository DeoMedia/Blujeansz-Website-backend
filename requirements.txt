# Pinned to versions that publish prebuilt wheels for Python 3.13/3.14.
# Older pins (pydantic-core, asyncpg, greenlet) have no 3.14 wheels and force a
# Rust/C source build, which fails on a machine without a toolchain.
fastapi==0.141.1
uvicorn[standard]==0.52.1
pydantic==2.13.4
pydantic-settings==2.15.0
email-validator==2.2.0
sqlalchemy==2.0.51
asyncpg==0.31.0
greenlet==3.5.4
httpx==0.28.1
pyjwt[crypto]==2.13.0
python-multipart==0.0.32
